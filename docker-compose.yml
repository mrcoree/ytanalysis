services:
  api:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4 --timeout-keep-alive 65
    restart: always
    mem_limit: 1g
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://viral:${POSTGRES_PASSWORD}@postgres:5432/viral_radar
      - REDIS_URL=redis://redis:6379/0
      - SUBTITLE_PROXY_URL=${SUBTITLE_PROXY_URL}
      - JWT_SECRET=${JWT_SECRET}
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    volumes:
      - .:/app

  worker:
    build: .
    command: celery -A app.celery_app:celery worker --beat --loglevel=info --schedule=/tmp/celerybeat-schedule
    restart: always
    mem_limit: 512m
    environment:
      - DATABASE_URL=postgresql://viral:${POSTGRES_PASSWORD}@postgres:5432/viral_radar
      - REDIS_URL=redis://redis:6379/0
      - JWT_SECRET=${JWT_SECRET}
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    volumes:
      - .:/app

  postgres:
    image: postgres:16-alpine
    restart: always
    mem_limit: 256m
    environment:
      - POSTGRES_USER=viral
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=viral_radar
    ports:
      - "127.0.0.1:5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U viral -d viral_radar"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: always
    mem_limit: 128m
    ports:
      - "127.0.0.1:6379:6379"

  tunnel:
    image: cloudflare/cloudflared:latest
    command: tunnel --no-autoupdate run --token eyJhIjoiNmU5YmJiNjE0ZGUwODllOWMzYjUzM2VlYjMyZTU4M2EiLCJ0IjoiOWU0YTUxNTQtYjgwNS00ODBlLWI1NmItNjlhOGE3NzFhMjkxIiwicyI6IlpHWTRNVFUwT1RRdFptRXlZeTAwWW1ZeUxXRTFNV1l0WXpNME56WmtPVGhsTnpRMyJ9
    depends_on:
      - api
    restart: always

volumes:
  pgdata:
